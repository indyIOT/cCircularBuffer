add_executable(${EXECUTABLE_NAME}  source/main.c)

target_include_directories(${EXECUTABLE_NAME} PRIVATE include)
target_include_directories(${EXECUTABLE_NAME} PRIVATE ../../cErrorDriver/cErrorDriverLib/publicInclude)
target_include_directories(${EXECUTABLE_NAME} PRIVATE ../cCircularBufferLib/publicInclude)
target_include_directories(${EXECUTABLE_NAME}  PRIVATE source)

target_link_libraries(${EXECUTABLE_NAME} PUBLIC cErrorDriverLib )
target_link_libraries(${EXECUTABLE_NAME} PUBLIC ${CIRCULAR_BUFFER_LIBRARY_NAME} )
target_link_libraries(${EXECUTABLE_NAME} PUBLIC commonMacros )
